# v0.11-finance Preparation Handoff

**Reviewed:** 2026-08-28
**Branch:** `release/v0.11-finance-prep`
**Baseline commit:** `ceba7a34102d43218700d59882435ca5ec214857`
**Release profile:** `v0.11-finance`
**Migration ceiling:** `000129_ap_exception_resolution_events`
**Status:** preparation candidate only; working tree has uncommitted prep changes; not a release tag and not production-certified

This is the bounded handoff for the Treasury + P2P work that follows the immutable
`v0.10.0-rc.7` core line. It must remain separate from the rc.7 promotion record
and use the isolated finance sandbox. Asset locations, custody/transfers,
warranty/maintenance extensions, and capitalization remain outside this tranche.

The recorded commit is the clean preparation baseline. The current isolated working
tree adds provider-router fail-closed behavior, worker AP dependency wiring, exact
goods-return quantity checks, settlement-result consistency checks, an AP exception
workbench with tenant/RBAC guards, and immutable resolution events in migration
`000129`; publish a new commit and regenerate the candidate identity before dispatching
the sandbox workflow.

## Candidate and dispatch checklist

- [x] Record the exact branch, clean preparation baseline, release profile, and migration ceiling.
- [x] Keep the v0.10-core candidate boundary at migration `000124`; v0.11
      preparation migrations begin after that boundary.
- [ ] Publish the intended branch, tag, or commit before dispatching the sandbox
      workflow. Supply an explicit `candidate_ref`; never rely on a staging default
      or an unpublished local branch.
- [ ] Confirm the checked-out SHA, profile, and migration ceiling in generated
      release identity and artifact metadata.

## Local implementation updates

- [x] Bank-feed sync and webhook paths resolve through an explicit provider router
      and fail closed before creating durable records when no approved adapter is
      registered.
- [x] Worker AP processing validates and wires accounting, tax, and FX boundaries;
      unsupported settlement effects remain explicitly fail closed.
- [x] Goods-return cumulative quantity checks use the schema's exact four-decimal
      domain and reject non-finite legacy float inputs.
- [x] Goods-receipt intake rejects supplier/product/line-shape violations and
      over-quantity using exact four-decimal comparisons; cross-GRN allocation is
      serialized by a locked PO row and a cumulative draft/posted receipt query.
- [x] Settlement results reject provider status/local-state mismatches before any
      accounting-effect request can be accepted.
- [x] AP exceptions have a bounded SSR list/resolve workflow with dedicated RBAC,
      CSRF-protected terminal actions, SQL-native company filtering before pagination,
      linked-invoice defense-in-depth, and immutable tenant-scoped resolution events
      with optional comments in migration `000129`.
- [x] The post-change serial `go test -p=1 ./... -count=1`, targeted `go vet`,
      production-tagged application/worker builds, and changed-package lint all pass.
      The opt-in PostgreSQL concurrent receipt test is present and skips cleanly without
      a DSN; a live database run and provider sandbox evidence remain release gates.

## Evidence gate

Passing local tests does not satisfy a staging or production gate. The following
evidence remains open:

| Area | Required evidence | State |
| --- | --- | --- |
| Profile and migrations | `v0.11-finance` route/config contract, up/down rehearsal, exact artifact identity, route manifest | Open |
| Bank feeds | Supported provider adapter, scheduled sync/recovery, callback replay, consent/backoff, and company-isolation scenarios | Open |
| Payment execution | Worker composition, provider sandbox execution, ambiguous/partial/failure recovery, and durable idempotency evidence | Open |
| Settlement accounting | Exact AP allocation, FX, tax, journal, bank, and GL effects without duplicate settlement | Open |
| P2P | Requisition/PO through receipt, matching, AP, approval, payment, and ledger scenarios with exact-money/database-backed tolerance cases | Open |
| Security and operations | Scoped-access negatives, backup/restore, rollback, queue/worker observation, alerts, and operator runbooks | Open |
| Decision record | Immutable evidence index, approver sign-off, and explicit decision to keep live execution disabled | Open |

## Required handoff artifacts

- [ ] Candidate SHA, migration ceiling, profile, checksums, provenance, and SBOM
- [ ] Migration rehearsal and rollback/restore results from the isolated sandbox
- [ ] Deterministic unit, integration, provider-sandbox, and database-backed results
- [ ] Route manifest, company/branch isolation, permission, and audit evidence
- [ ] Provider, worker, settlement, P2P, recovery, metrics, and operator-runbook evidence
- [ ] Immutable evidence index with findings, owners, remediation state, and approval

Until every gate is recorded, keep `production-certified=no`, keep live provider
execution and company feature flags disabled by default, and do not promote this
preparation candidate as a v0.11 release.

See the [feature matrix](../reference/feature-matrix.md) and
[core finance automation plan](../guides/core-finance-automation-plan.md) for the
detailed contracts and release criteria. The sandbox-specific deployment guide
lives on the isolated preparation branch until that candidate is published.

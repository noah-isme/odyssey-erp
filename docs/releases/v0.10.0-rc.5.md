# Odyssey ERP v0.10.0-rc.5 — 2026-08-12

**Status:** Release candidate; not production-certified.

**Application baseline:**
`ec65cc08639c184030c63e3407791987eee92804`

**Candidate commit:** Resolved by the annotated `v0.10.0-rc.5` tag after this
release packaging is committed.

**Release profile:** `v0.10-core`

**Migration ceiling:** `000124_scoped_rbac_global_compatibility`

This candidate supersedes rc.4 while retaining the same reviewed application
baseline and bounded v0.10-core scope. The annotated rc.5 tag is the authority
for the exact packaging commit. Commit
`1a8343e4499420467ba3dda04a2683782c6c79d7`, migration
`000125_payment_settlement_results`, and v0.11-only finance routes remain
excluded.

## Changes since rc.4

- Recorded rc.4 as an immutable failed candidate after CI run
  `31610988259` failed in the E2E route sweep.
- Retargeted the active roadmap, release checklist, certification template,
  deployment runbook, and current-candidate references to rc.5.
- Preserved the component checksums, migration and web manifests, SPDX SBOM,
  content-addressed release bundle, and GitHub build-provenance evidence model.

## Verification and remaining gates

The rc.5 commit must pass CI, including the complete E2E route sweep, before
the tag-triggered staging workflow may build or deploy it. Deploy and certify
only the commit resolved by the annotated `v0.10.0-rc.5` tag with
`RELEASE_PROFILE=v0.10-core`.

The five core journeys, migration and backup restore, scoped-access negative
tests, operational controls, rollback, and required profile-scoped external
gates remain open until their evidence is recorded. Until those gates and the
release-owner go/no-go decision pass, all v0.10-core feature-matrix rows remain
`production-certified=no`.

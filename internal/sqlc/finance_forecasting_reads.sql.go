// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: finance_forecasting_reads.sql

package sqlc

import (
	"context"
)

const getLatestForecastRun = `-- name: GetLatestForecastRun :one
SELECT id, company_id, scenario_id, status, fx_snapshot, completed_at, error_details, created_at FROM forecast_runs 
WHERE company_id = $1 AND scenario_id = $2 AND status = 'COMPLETED' 
ORDER BY completed_at DESC LIMIT 1
`

type GetLatestForecastRunParams struct {
	CompanyID  int64 `json:"company_id"`
	ScenarioID int64 `json:"scenario_id"`
}

func (q *Queries) GetLatestForecastRun(ctx context.Context, arg GetLatestForecastRunParams) (ForecastRun, error) {
	row := q.db.QueryRow(ctx, getLatestForecastRun, arg.CompanyID, arg.ScenarioID)
	var i ForecastRun
	err := row.Scan(
		&i.ID,
		&i.CompanyID,
		&i.ScenarioID,
		&i.Status,
		&i.FxSnapshot,
		&i.CompletedAt,
		&i.ErrorDetails,
		&i.CreatedAt,
	)
	return i, err
}

const listForecastDailyBucketsByRun = `-- name: ListForecastDailyBucketsByRun :many
SELECT id, run_id, bank_account_id, currency, bucket_date, opening_balance, total_inflow, total_outflow, closing_balance, created_at FROM forecast_daily_buckets WHERE run_id = $1 ORDER BY bucket_date ASC
`

func (q *Queries) ListForecastDailyBucketsByRun(ctx context.Context, runID int64) ([]ForecastDailyBucket, error) {
	rows, err := q.db.Query(ctx, listForecastDailyBucketsByRun, runID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ForecastDailyBucket
	for rows.Next() {
		var i ForecastDailyBucket
		if err := rows.Scan(
			&i.ID,
			&i.RunID,
			&i.BankAccountID,
			&i.Currency,
			&i.BucketDate,
			&i.OpeningBalance,
			&i.TotalInflow,
			&i.TotalOutflow,
			&i.ClosingBalance,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
